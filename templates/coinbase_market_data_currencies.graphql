query {
  coinbaseMarketDataCurrencies {
    name
    id
    minSize
    status
    message
    maxPrecision
    convertibleTo
    displayName
    processingTimeSeconds
    minWithdrawalAmount
    maxWithdrawalAmount
  }
}
